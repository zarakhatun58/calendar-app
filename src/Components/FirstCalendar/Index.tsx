import React from 'react';

const FirstCalendar = () => {
    return (
        <div>
            <h4 style={{color:'green', fontSize:'20px'}}> Calendar</h4>
        </div>
    );
};

export default FirstCalendar;